module.exports = (db) => {
  // db.user.hasOne(db.userProfile, {
  //   foreignKey: { name: "userId", unique: true },
  // });
  // db.userProfile.belongsTo(db.user, {
  //   foreignKey: { name: "userId", unique: true },
  // });
};
